[build-system]
requires = ["flit_core >=3.2,<4", 'stochastic','numpy','tqdm']
build-backend = "flit_core.buildapi"

[project]
name = "comb-model"
authors = [{name = "Luiz G. A. Alves", email = "lgaalves@northwestern.edu"}]
dynamic = ["version", "description"]